@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="panel panel-default">
                    <div class="panel-heading">Borrow {{ $borrow->id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('/borrow/borrow') }}" title="Back"><button class="btn btn-warning btn-xs"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/borrow/borrow/' . $borrow->id . '/edit') }}" title="Edit Borrow"><button class="btn btn-primary btn-xs"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['borrow/borrow', $borrow->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<i class="fa fa-trash-o" aria-hidden="true"></i> Delete', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete Borrow',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $borrow->id }}</td>
                                    </tr>
                                    <tr><th> Student </th><td> {{ $borrow->Student->name }} </td></tr><tr><th> Book</th><td> {{ $borrow->Book->title }} </td></tr><tr><th> Borrow Date </th><td> {{ $borrow->borrow_date }} </td></tr><tr><th> Return Date </th><td> {{ $borrow->return_date }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
